import { GenerateArrayPipe } from './generate-array.pipe';

describe('GenerateArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
